services:
  amikan-sshges:
    build: .
    container_name: amikan-sshges
    env_file:
      - .env.production
    ports:
      - "3000:3000"
    restart: always
    networks:
      - webnet

networks:
  webnet:
    external: true
